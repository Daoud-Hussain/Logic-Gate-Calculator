/**
 * @format
 */

import {AppRegistry} from 'react-native';
import NandCalculator from './NandCalculator';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => NandCalculator);
